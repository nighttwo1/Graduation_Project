using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecogButtonScript : MonoBehaviour
{
    public static GameObject recogBtn;

    // Start is called before the first frame update
    void Start()
    {
        recogBtn = GameObject.Find("Button");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
